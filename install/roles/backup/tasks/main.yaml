---
- name: Check if backup repo exists
  ansible.builtin.stat:
    path: ~/backup
  register: backup_exists

- name: BACKUP
  ansible.builtin.git:
    version: main
    repo: https://github.com/G33KM44N38/backup
    update: false
    dest: ~/backup
  when: not backup_exists.stat.exists
