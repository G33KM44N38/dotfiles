# Computer use

One job: limit computer-use access to explicit requests.

## Rules

- Use the `computer-use` skill and its tools only when the user explicitly requests computer use.
- Do not use computer use implicitly or as a fallback.
